# docs

Place screenshots referenced by the root `README.md` here:

- `screenshot-inspector.png` — the explorer with a cell selected and overlays on
- `screenshot-playground.png` — the playground showing a `gridDisk` / polygon fill

To capture them, run the stack (`docker compose up --build` or the local dev
servers) and grab the two views at a comfortable window size (~1440×900).
